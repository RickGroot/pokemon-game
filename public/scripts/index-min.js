let socket=io(),socketConnection=io.connect(),room=document.getElementById("room").innerHTML,user=document.getElementById("user").innerHTML,userList=document.getElementById("userList"),btn=document.getElementById("test");socketConnection.on("connect",()=>{let e={name:user,userId:socket.id,room:room,score:0};socket.emit("join",e)}),socket.on("userList",e=>{console.log("client got userList",e),userList.innerHTML="",e.forEach(e=>{userList.innerHTML+="<p>"+e.userName+"</p>"})}),socket&&console.log("socket connected to client");